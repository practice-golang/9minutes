APP_NAME = 9minutes

[server]
ADDRESS = 127.0.0.1
PORT = 5525
; ADDRESS = 0.0.0.0
; PORT = 80

[dirpaths]
STATIC_PATH = ./static
UPLOAD_PATH = ./upload
HTML_PATH = ./html

[session]
STORE_TYPE = memstore

; [session]
; STORE_TYPE = etcd
; ADDRESS = localhost
; PORT = 2379

; [session]
; STORE_TYPE = redis
; ADDRESS = localhost
; PORT = 6379


[database]
DBTYPE = sqlite
FILEPATH = ./9minutes.db

; [database]
; DBTYPE = mysql
; ADDRESS = 127.0.0.1
; PORT = 13306
; USER = root
; PASSWORD = 
; DATABASE = 9m

; [database]
; DBTYPE = sqlserver
; ADDRESS = 127.0.0.1
; PORT = 11433
; USER = sa
; PASSWORD = SQLServer1433
; DATABASE = 9m
; SCHEMA = dbo

; [database]
; DBTYPE = postgres
; ADDRESS = 127.0.0.1
; PORT = 5432
; USER = root
; PASSWORD = pgsql
; DATABASE = 9m
; SCHEMA = public

; [database]
; DBTYPE = postgres
; ADDRESS = 127.0.0.1
; PORT = 5432
; USER = root
; PASSWORD = pgsql
; DATABASE = postgres
; SCHEMA = 9m

; email : Purpose of sending id verification when joining
; In order to avoid SPAM blocking by email servers, dkim and spf should be set when send a verification email directly
; The domain of FROM_ADDRESS and ip address must be findable from spf record
[email]
USE_EMAIL = true
SEND_DIRECT = true
DOMAIN = http://localhost:5525 ; DOMAIN for link in email body
DKIM_PATH = ./dkim.key
FROM_ADDRESS = no_reply@domain.ext
FROM_NAME = Site manager
FROM_ADDRESS = no_replay@domain.ext

; [email]
; USE_EMAIL = true
; SEND_DIRECT = false
; DOMAIN = "http://localhost:5525" ; DOMAIN for link in email body
; SERVER = smtp.gmail.com
; PORT = 587
; USER = myid ; or myid@mydomain.com
; PASSWORD =
; FROM_NAME = Site manager
; FROM_ADDRESS = no_replay@mydomain.com
